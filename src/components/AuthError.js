import React from 'react'

export const AuthError = ({ error }) => {
    if (!error) {
        return (
            <>
            </>
        )
    }
    else {
        return (
            <div>
                
            </div>
        )
    }
}
